<% html_title l('testing_steps.generate_title', :version => @version.name) %>

<%= render :partial => 'warnings',
  :locals => { :version => @version, :project => @project } %>

<div style="width:100%;">
<h2><%= l('testing_steps.generate_title', :version => @version.name) %></h2>
<p>
<%= link_to l('testing_steps.raw'), :raw => "true",
  :testing_steps_format => params[:testing_steps_format] %> | 
<%= link_to l('testing_steps.download'), :download => "true",
  :testing_steps_format => params[:testing_steps_format] %>
</p>

<%= render_other_formats(@formats) %>

<%= render :partial => 'generated_testing_steps' %>
