<% if @formats.any? %>
  <table class='list'>
    <thead>
      <th><%= t('testing_steps.formats.title') %></th>
      <th></th>
    </thead>
    <tbody>
      <% @formats.each do |format| %>
        <tr class='<%= cycle 'odd', 'even'%>'>
          <td class='name'>
            <%= link_to h(format.name), edit_testing_steps_format_path(format) %>
          </td>
          <td class='buttons'>
            <%= delete_link format %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <div class='nodata'><%= l(:label_no_data) %></div>
<% end %>

<p>
<%= link_to l('testing_steps.formats.new'), new_testing_steps_format_path, :class => 'icon icon-add' %>
</p>
